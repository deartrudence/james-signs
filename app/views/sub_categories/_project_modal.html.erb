
	<div class="modal open" id="project-modal">
	  <span class="close" id="close">✕</span>
	 		<div class="modal-carousel">
			<% @project.pictures.each do |picture| %>
				<div class="carousel-cell">
					<div class="carousel-image" style="background: url(<%= picture.image.url if picture.present? %>)">		
					</div>
					<div class="carousel-info wrapper--flex">
						<div class="left col--two">
							<h3><%= @project.title %></h3>	
							<p><%= @project.description %></p>
						</div>
						<div class="right col--two">
						</div>
					</div>
				</div>
			<% end %>
		</div>
	</div>